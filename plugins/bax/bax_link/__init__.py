"""Протокол Бакса и соединение с сервером — своя копия внутри плагина.

Claude Code ставит плагин, копируя его папку в свой кэш: всё, что лежит вне неё, туда
не попадает. Поэтому код подключения живёт здесь, а не берётся из движка
[bax-claude-code-agent](https://github.com/baxassist/bax-claude-code-agent).
"""

__version__ = "0.4.0"

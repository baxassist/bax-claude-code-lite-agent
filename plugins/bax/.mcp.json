{
  "mcpServers": {
    "bax": {
      "command": "uv",
      "args": [
        "run", "--no-project", "--quiet",
        "--with", "mcp==2.2.0",
        "--with", "websockets==17.1",
        "python", "${CLAUDE_PLUGIN_ROOT}/server.py"
      ]
    }
  }
}

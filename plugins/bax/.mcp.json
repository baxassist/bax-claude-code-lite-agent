{
  "mcpServers": {
    "bax": {
      "command": "uv",
      "args": [
        "run", "--no-project", "--quiet",
        "--with", "mcp>=1.0",
        "--with", "websockets>=15.0",
        "python", "${CLAUDE_PLUGIN_ROOT}/server.py"
      ]
    }
  }
}

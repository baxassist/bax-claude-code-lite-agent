{
  "mcpServers": {
    "bax": {
      "command": "uv",
      "args": [
        "run", "--no-project", "--quiet",
        "--with", "mcp>=2.2,<3",
        "--with", "websockets>=15.0,<18",
        "python", "${CLAUDE_PLUGIN_ROOT}/server.py"
      ]
    }
  }
}
